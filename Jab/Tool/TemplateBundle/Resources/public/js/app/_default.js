/**
 * Default module - this module gets required for every route that does not have its own module
 * this is for testing only - this module should not do anything really
 */
define([], function() {
    console.log("_default");
});